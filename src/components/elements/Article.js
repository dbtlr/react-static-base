
import styled from 'styled-components';

const Article = styled.article`
  font-size: 1.2rem;
  line-height: 2rem;
  
  &.medium-text {
    font-size: 1.5rem;
  }
  
  &.large-text {
    font-size: 2.5rem;
  }
`;

export default Article;
